# خطة المشروع الشاملة: نظام حجز Aura Spa 🌟

هذه الوثيقة تمثل خارطة الطريق الكاملة للمشروع، تفصل كل جزء من أجزاء النظام، الميزات، الأهداف، والهيكل التقني. تم تصميم هذه الخطة ليس فقط لبناء تطبيق فعال، ولكن لإبراز مهاراتك التقنية العميقة في أي مقابلة عمل.

---

## 🏗️ الهيكل العام للمشروع والتقنيات (Tech Stack)

*   **إطار العمل الواجهة الأمامية (Frontend):** React.js مع Vite لسرعة البناء.
*   **نظام التصميم (Design System):** تصميم مخصص يعتمد على أسلوب **Riso Print** (ألوان ثابتة، ظلال قاسية، حواف 4px) مبرمج بالكامل عبر CSS Variables في `index.css` دون استخدام مكتبات جاهزة (يثبت قدرتك على بناء Design Systems).
*   **قاعدة البيانات والواجهة الخلفية (Backend/BaaS):** **Supabase** (PostgreSQL). خيار احترافي يوفر API جاهز ومزامنة فعلية للبيانات بين العميلة وإدارة الصالون.
*   **إدارة التوجيه (Routing):** `react-router-dom` للتنقل بين صفحة الهبوط، الحجز، ولوحة التحكم.

### هيكل المجلدات (Directory Structure)
```text
src/
├── components/
│   ├── layout/      # Navbar, Footer
│   ├── sections/    # Hero, Services, Reviews, Contact
│   ├── ui/          # المكونات القابلة لإعادة الاستخدام (Button, Card, Input, Badge, Alert)
│   ├── booking/     # [جديد] مكونات مسار الحجز (Calendar, TimeSlots, BookingForm)
│   └── dashboard/   # [جديد] مكونات لوحة الإدارة (Stats, BookingsTable, ServicesManager)
├── data/            # البيانات الثابتة (سيتم استبدالها لاحقاً بـ Supabase)
├── hooks/           # [جديد] Custom Hooks (مثل useBookings, useServices)
├── lib/             # [جديد] إعدادات الاتصال (مثل supabase.js)
├── pages/           # [جديد] الصفحات الرئيسية (Home, BookingPage, Dashboard, Login)
├── App.jsx          # الموجه الرئيسي (Router)
└── index.css        # نظام التصميم والمتغيرات
```

---

## 🎯 الجزء الأول: صفحة الهبوط (Landing Page) - *تم الإنجاز*

*   **الهدف:** واجهة ترحيبية تعرض هوية الصالون وتجذب العميلات.
*   **الميزات المنجزة:**
    *   قسم Hero تفاعلي.
    *   عرض الخدمات مقسمة حسب الفئات.
    *   آراء العميلات مع نظام إعجاب تفاعلي.
    *   معلومات التواصل مع خريطة Riso SVG تفاعلية.

---

## 📅 الجزء الثاني: ميزات نظام الحجز (الجزء التقني الأهم)

هذا الجزء هو جوهر التطبيق بالنسبة للعميلة، ويثبت قدرتك على التعامل مع المنطق المعقد (Complex Logic).

| الميزة | الهدف منها | الفائدة (ما يثبته في المقابلة) |
| :--- | :--- | :--- |
| **اختيار الخدمة (قائمة/بطاقات)** | تحديد مدة وسعر الحجز قبل أي شيء. | يحدد منطق حساب الأوقات المتاحة لاحقاً (ديناميكية التطبيق). |
| **تقويم تفاعلي (أيام متاحة فقط)** | منع الحجز في أيام العطلة (الجمعة مثلاً). | يثبت مهارة التعامل مع التواريخ في JS (Date object) وتخصيص UI التقويم. |
| **عرض الأوقات المتاحة فقط** | منع تعارض الحجوزات (Double Booking). | **أقوى نقطة منطقية بالمشروع** — تُحسب ديناميكياً بناءً على الحجوزات الموجودة مسبقاً في قاعدة البيانات ومدة الخدمة المطلوبة. |
| **فورم بيانات العميلة** | جمع بيانات التواصل (اسم، هاتف، ملاحظات) لتأكيد الحجز. | يثبت مهارة Form Validation (رقم هاتف صحيح، الحقول المطلوبة، إدارة حالة الـ Form). |
| **شاشة تأكيد الحجز** | إعطاء راحة نفسية للعميلة بأن الحجز تم بنجاح. | يثبت فهم إدارة الحالة بعد إرسال البيانات (Success State) وتجربة المستخدم (UX). |
| **رسائل الخطأ الواضحة** | مثلاً "هذا الوقت أصبح محجوزاً، اختر وقتاً آخر". | يثبت مهارة Error Handling الجيدة التي يبحث عنها المحاورون لمعرفة قدرتك على التعامل مع Edge Cases. |

---

## 📊 الجزء الثالث: ميزات Dashboard الإدارة (نقطة تميزك الحقيقية)

لوحة التحكم تثبت أنك لست مجرد مطور واجهات، بل مطور قادر على بناء أنظمة إدارة متكاملة تعتمد على بيانات حقيقية.

| الميزة | الهدف منها | الفائدة (ما يثبته في المقابلة) |
| :--- | :--- | :--- |
| **تسجيل دخول بسيط للإدارة** | حماية بيانات العميلات من العامة. | يثبت فهمك لمفهوم Protected Routes والـ Authentication الأساسي. |
| **إحصائيات سريعة** | مساعدة صاحبة العمل في اتخاذ قرارات سريعة (حجوزات الأسبوع، الخدمة الأكثر طلباً). | يثبت القدرة على إجراء حسابات منطقية (Logic) وتجميع البيانات (Data Aggregation). |
| **عرض كل الحجوزات (جدول/قائمة)** | رؤية شاملة لصاحبة الصالون لسير العمل. | يثبت عملية Read من قاعدة البيانات وعرض بيانات منظمة (Data Tables). |
| **فلترة الحجوزات** | سرعة الوصول (اليوم، الأسبوع، حسب الخدمة، حسب الحالة). | يثبت القدرة على بناء منطق Filter/Sort متقدم على مصفوفات البيانات الكبيرة. |
| **تأكيد / إلغاء الحجز** | إدارة فعلية للعمل اليومي (تغيير status). | يثبت عملية Update (CRUD) — أهم إجراء تفاعلي في الـ Dashboard. |
| **حذف حجز** | تنظيف البيانات القديمة/الملغاة تماماً. | يثبت عملية Delete (اكتمال دائرة الـ CRUD الكاملة). |
| **إدارة الخدمات** | مرونة تحديث قائمة الخدمات (إضافة/تعديل/حذف خدمة وسعرها) دون لمس الكود. | يثبت فهمك العميق لـ CRUD على جداول متعددة (جدول services). |

---

## 💆‍♀️ الجزء الرابع: الخدمات المقترحة (بيانات تجريبية واقعية)

استخدام بيانات واقعية بمدد زمنية مختلفة (`duration_minutes`) هو **حركة تقنية ذكية جداً** ستلفت انتباه أي تقني يراجع مشروعك، لأنها تجعل خوارزمية "الأوقات المتاحة" معقدة وحقيقية.

| الخدمة | المدة التقريبية (`duration`) | الهدف منها (للعميلة) | الفائدة (للصالون) |
| :--- | :--- | :--- | :--- |
| **قص شعر** | 30-45 دقيقة | تجديد المظهر بسرعة | خدمة سريعة ومتكررة = دخل ثابت |
| **صبغة شعر** | 90-120 دقيقة | تغيير جذري بالمظهر | سعر مرتفع = ربح أعلى للحجز الواحد |
| **تسريحة / بلو دراي** | 30-60 دقيقة | مناسبات وفعاليات | طلب موسمي مرتفع (أعراس، مناسبات) |
| **مكياج** | 45-60 دقيقة | مناسبات خاصة | يرتبط غالباً بخدمات أخرى (Up-sell طبيعي) |
| **عناية بالبشرة (فيشل)** | 45-60 دقيقة | استرخاء + صحة البشرة | عملاء يعودون بشكل دوري (Recurring) |
| **منكير / باديكير** | 30-45 دقيقة | عناية وتجميل الأطراف | خدمة إضافية تزيد متوسط الفاتورة |
| **إزالة الشعر (Waxing)** | 20-40 دقيقة | نتيجة تدوم أطول | حجوزات دورية شهرية تقريباً |

---

## 🗄️ بنية قاعدة البيانات (Supabase PostgreSQL Schema)

لبناء هذا النظام، سنحتاج مبدئياً إلى جدولين رئيسيين في Supabase:

### 1. جدول الخدمات (`services`)
*   `id`: UUID (Primary Key)
*   `name`: Text (اسم الخدمة)
*   `category`: Text (الفئة)
*   `price`: Numeric (السعر)
*   `duration_minutes`: Integer (مدة الخدمة بالدقائق - **المفتاح السحري للمشروع**)
*   `description`: Text (الوصف)
*   `is_active`: Boolean (لإخفاء خدمة مؤقتاً دون حذفها)

### 2. جدول الحجوزات (`bookings`)
*   `id`: UUID (Primary Key)
*   `created_at`: Timestamp (تاريخ إنشاء الحجز)
*   `service_id`: UUID (Foreign Key يربط بجدول الخدمات)
*   `client_name`: Text (اسم العميلة)
*   `client_phone`: Text (رقم الهاتف)
*   `booking_date`: Date (تاريخ الحجز المختار)
*   `booking_time`: Time (وقت بداية الحجز المختار)
*   `status`: Text (حالة الحجز: `pending`, `confirmed`, `cancelled`, `completed`)
*   `notes`: Text (ملاحظات إضافية)

---

## 🚀 منهجية التنفيذ للخطوات القادمة

1.  **إعداد Supabase:** ربط المشروع، إنشاء الجداول، وإضافة البيانات التجريبية للخدمات.
2.  **بناء واجهة الحجز (Booking Flow):** تصميم الصفحات وتطبيق منطق التواريخ والأوقات (بدون إرسال بيانات أولاً).
3.  **ربط الحجز بالبيانات:** جعل واجهة الحجز تقرأ الخدمات المتاحة من Supabase وترسل الحجز الجديد.
4.  **بناء واجهة الإدارة (Dashboard):** تصميم وبناء الجداول والإحصائيات وجلب البيانات الحية من Supabase.
5.  **تطبيق CRUD وتغيير الحالة:** إضافة أزرار التأكيد، الإلغاء، والحذف في لوحة التحكم وتحديث قاعدة البيانات لحظياً.
